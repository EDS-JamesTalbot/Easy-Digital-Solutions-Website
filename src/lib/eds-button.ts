/**
 * Primary CTA — green background, white label (all main action buttons site-wide).
 * Uses !text-white so inherited body colour never overrides the label.
 */
export const edsPrimaryButtonClass =
  "inline-flex items-center justify-center rounded-full bg-eds-green px-6 py-3 text-center text-sm font-semibold !text-white no-underline shadow-sm transition hover:bg-eds-green-dark hover:!text-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-eds-green";
